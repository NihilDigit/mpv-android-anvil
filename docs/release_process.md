Release Process
===============

This is a research demo app (ANVIL VFI Demo). There is no formal release process.

Builds are produced via `buildscripts/buildall.sh --arch arm64` and distributed as debug APKs for testing purposes.
